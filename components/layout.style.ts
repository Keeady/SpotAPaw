import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  logo: {
    width: 100,
    marginLeft: 0,
    resizeMode: "contain",
    height: 50,
    backgroundColor: "red"
  },
  button: {
    marginRight: 12,
  },
});

export default styles;